<div id="display" rel="<%= preso.id() %>">
  <div id="slides">
  <% $.each(preso.attributes.slides, function(i, slide) { %>
    <% if (slide) { %>
    <div class="slide active <%= slide.theme %>" id="slide-<%= slide.position %>" style="<%= slide.additional_css %>">
      <div class="content">
<%= slide.content_html %>
      </div>
    </div>
    <% } %>
  <% }); %>
  </div>
</div>