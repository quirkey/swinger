<div id="display" rel="<%= preso.id() %>">
  <div id="slides">
  <% $.each(preso.attributes.slides, function(i, slide) { %>
    <% if (slide) { %>
    <div class="slide basic" id="slide-<%= slide.position %>">
      <div class="content">
<%= slide.content_html %>
      </div>
    </div>
    <% } %>
  <% }); %>
  </div>
</div>